{{> header}}

<div class="content">
    {{#if loggedIn}}
        {{> menu}}
    {{/if}}
   <div class="posts">
    {{#each posts}}
            {{> post}}
    {{else}}
        <p>No posts in database</p>
    {{/each}}
   </div>
</div>

{{> footer}}