<article class="post post-content">
    <p>{{content}}</p>
    <div class="info">
        submitted {{interval}} ago by {{author}} | {{#if isAuthor}}<a href="#/comments/{{postId}}/delete/{{_id}}" class="deleteLink">delete</a> {{/if}}
    </div>
</article>